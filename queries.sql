--key       =     "'"+str(fac_id)+"'"
--checkPrimaryKey     =     "SELECT count(*) from faculty_tab where fac_id = "+ key+';'


--insert new professor
--"INSERT INTO faculty_tab (name, email, phone, address, password, fac_id) VALUES (%s, %s, %s, %s, %s, %s);", 
   -- (name, email, phone, address, password, fac_id)


-- check login
-- queryPassword = "'"+str(password)+"'"
    --queryusername = "'"+str(username)+"'"
    --query = "Select count(*) from faculty_tab where fac_id = " + queryusername + ";"
